version: "2.2"
services:
 blazegraph:
  container_name: "${COMPOSE_PROJECT_NAME}-blazegraph"
  image: "${BLAZEGRAPH_IMAGE}"
  restart: unless-stopped
  volumes:
     - /mnt/blazegraph-data:/blazegraph-data
  environment:
     - JAVA_OPTS=-Xmx1g
  logging:
   driver: json-file
   options:
    max-size: "200k"
    max-file: "10"

 metaphactory:
  image: "${METAPHACTORY_IMAGE}"
  volumes:
    - /mnt/rds/rds-global:/rdsapps/rds-global
  environment:
     - PLATFORM_OPTS=-Dconfig.environment.sparqlEndpoint=http://${COMPOSE_PROJECT_NAME}-blazegraph:8080/blazegraph/sparql ${METAPHACTORY_OPTS}
  depends_on:
       - blazegraph


volumes:
  blazegraph-data:
